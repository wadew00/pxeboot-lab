#!ipxe
set base http://${next-server}:@@PXE_HTTP_PORT@@
set fedora_tree @@FEDORA_TREE_URL@@
kernel ${fedora_tree}/images/pxeboot/vmlinuz ip=dhcp inst.repo=${fedora_tree} inst.sshd inst.text inst.ks=${base}/fedora.ks
initrd ${fedora_tree}/images/pxeboot/initrd.img
boot
